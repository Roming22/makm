#!/bin/bash -e
# This file should be removed if the container is not run as a daemon.
#
# It should perform some kind of validation that the container is up and running.
# For example, if the container is serving webpages:
#   curl -s --fail -k https://127.0.0.1:8000/ > /dev/null

# TODO: [REPLACE]
# Do not fail silently until the script has been modified
exit 1
